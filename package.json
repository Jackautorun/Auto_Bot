{
  "name": "auto-bot",
  "version": "0.0.0",
  "private": true,
  "description": "Automation workflows and tooling for the Auto_Bot repository",
  "license": "MIT",
  "scripts": {
    "eval": "node -e \"require('fs').mkdirSync('reports/latest', { recursive: true })\" && npx promptfoo eval promptfooconfig.yaml --report ./reports/latest"
  }
}
